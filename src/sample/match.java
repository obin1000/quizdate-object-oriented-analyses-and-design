package sample;

import java.util.List;

public class match {
    private List likes;
    public boolean checkIfLiked(User u, int ID){
        likes = u.getLikes();

        for(Integer i : likes){
            if (ID)
        }

        return true;
    }
}
